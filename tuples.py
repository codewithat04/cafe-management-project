#tuples are ordered but immutable collections
a=(3,5,7,90,'ninja')
print(a[4])
# a[3]=32#this will show error as tuples are immutable we use tuples bcoz we dont want our collections to be changesd.
b=(5,)#if you wanna use only one element you can use comma.
print(b)